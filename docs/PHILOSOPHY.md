# FastTLS Philosophy

FastTLS keeps secure transport explicit and composable: FastNet owns transport, FastTLS owns authentication and encryption, and provider-specific acceleration stays behind a stable Java contract.
